#include<stdio.h>

int main() {

    int temp;

    for (int i = 1; i <= 18; i++){
        printf("Ingrese una temperatura distinta de 0: ");
        scanf("%d", &temp);
        if (temp != 0) {
            
        }
    }

    return 0;
}